import { Text } from '@react-three/drei';
import { Canvas } from '@react-three/fiber';

const TextScreen = () => {
	return (
		<Canvas>
			<Text>Text</Text>
		</Canvas>
	);
};

export default TextScreen;
